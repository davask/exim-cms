twig:
    paths:
        '%exim.lcdd.search.resources%/views': views
    globals:
        lcdd:
            elastic:
                host: '%lcdd.elastic.host%'
                port: '%lcdd.elastic.port%'
                request: '%lcdd.elastic.request%'
                index: '%lcdd.elastic.index%'
            search:
                host: '%lcdd.search.host%'
                port: '%lcdd.search.port%'
                request: '%lcdd.search.request%'
