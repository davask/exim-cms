{% if app.user and is_granted('ROLE_EXIM_ADMIN') %}
    {%- if bname is defined -%}
<!-- exim {{bname}} -->
{# exim space fix #}
    {%- else -%}
<!-- exim -->
{# exim space fix #}
    {%- endif -%}
{% endif %}
